# Implementation Notes

HTML/JSX 디자인 프로토타입(`dashboard_design/`, `desktop_design/`)을 실제 동작하는 코드로 옮길 때 양 클라이언트 공통으로 지켜야 할 규칙입니다. 디자인에는 표현되지 않지만 코드에서는 반드시 처리해야 하는 사항을 모았습니다.

## 0. 시스템 핵심 개념

본 시스템은 **조직 단위로 운영되는 위키 + 데스크톱 작업 보조 도구**입니다. 디자인 파일에 보이는 회계법인/세무 도메인은 첫 번째 vertical(우리회계법인)일 뿐이며, 코어는 도메인 중립적으로 구현합니다.

### 0.1 핵심 엔티티
- **사용자(User)**: 인증된 계정. 이메일/비밀번호 또는 SSO로 로그인.
- **조직(Organization)**: 사용자가 소속되는 단위. 자체 위키와 멤버십을 가진다. 디자인의 "사무소"·"회계법인"은 모두 이 조직의 한 종류.
- **조직 위키(Org Wiki)**: 조직 단위로 구축·공유되는 위키. 사용자의 로컬 파일을 백엔드 파이프라인이 재구성해 만든 결과물.
- **타겟 폴더(Target Folder)**: 사용자가 데스크톱에서 지정한 로컬 폴더. 이 폴더 안의 파일들이 ingest 파이프라인의 입력이 된다.

### 0.2 핵심 흐름
1. 사용자가 로그인하고, 하나 이상의 조직에 소속된다.
2. 데스크톱에서 사용자가 타겟 폴더를 지정한다 (docx, hwp, xlsx, pdf, txt, md 등 다양한 형식 혼재).
3. 폴더 내 파일들이 백엔드 파이프라인을 거쳐 위키 노드로 재구성되고, 조직 위키에 반영된다.
4. 서버에는 각 조직의 위키가 격리·저장된다.
5. 웹 대시보드에서 조직·위키·사용자 정보를 열람·관리한다.
6. 데스크톱에서 작업 중에는 조직 위키와 LLM을 활용해 정보 검색·관련 파일 탐색·도메인 데이터 기반 질의응답이 가능하다.

### 0.3 코드와 도메인 분리
- 디자인에 등장하는 도메인 어휘(`회사`, `세무조정`, `감사조서`, `세법 변경 알림` 등)는 **vertical 레이어**에서 다룬다.
- 코어 모델(`User`, `Organization`, `Wiki`, `WikiNode`, `SourceFile`, `IngestJob`)은 도메인 중립적으로 설계.
- 조직별로 vertical 프리셋(템플릿·태그 스키마·외부 연동)을 적용해 디자인이 보여주는 회계 도메인 동작을 만들어 낸다.

## 1. 인증 · 사용자 · 조직 멤버십

### 1.1 로그인
- 이메일 + 비밀번호 기본, SSO(Google/Microsoft)는 조직 정책으로 강제 가능.
- 세션 토큰은 안전한 HttpOnly 쿠키 또는 OS keychain(데스크톱).
- 데스크톱은 `omelette`/Electron/Tauri 보안 저장소를 사용해 토큰을 저장.

### 1.2 사용자–조직 관계
- 한 사용자는 **여러 조직**에 소속될 수 있다(드물지만 컨설턴트·외부 감사인 등).
- 각 조직 내에서 사용자는 역할(role: owner/admin/member/guest)을 가진다.
- 디자인 사이드바 푸터의 "김민지 회계사 · 감사 1팀 · 매니저"는 (조직, 팀, 역할) 표현 — 팀은 조직 내 하위 그룹.
- 활성 조직(active org)은 전역 상태로, 모든 화면이 같은 조직 컨텍스트에서 동작.

### 1.3 조직 전환
- 모든 데이터(위키, 파일, 알림)는 조직 단위로 격리.
- 조직 전환 시 캐시·상태를 모두 리셋해야 함(다른 조직 데이터 누출 방지).
- 데스크톱은 활성 조직별로 다른 타겟 폴더를 가질 수 있음.

## 2. 디자인 파일 해석 원칙

### 2.1 정적 HTML/JSX는 "보기"일 뿐
디자인 파일에 표시된 데이터는 모두 **목업**입니다. 코드에서는:
- 모든 텍스트/숫자/목록은 데이터 모델에서 가져온 값으로 치환
- 빈 상태(empty state), 로딩 상태(loading), 에러 상태를 디자인에 없더라도 추가
- 권한별 렌더링 분기를 디자인에 없더라도 추가

### 2.2 클래스명/색상 토큰
디자인 파일의 CSS 변수와 클래스명은 가능한 한 보존:
- 색상은 시맨틱 토큰으로 (`--accent`, `--c-alert`, `--c-warn` 등 — `desktop-styles.css` 참조)
- 컴포넌트 클래스는 디자인 파일 명명 규칙 따름 (`ww-*` for 대시보드, IDE 스타일 prefix for 데스크톱)
- 테마(라이트/다크) 분기는 CSS 변수 레벨에서

### 2.3 인터랙션
디자인에 호버/포커스/액티브 상태가 보이지 않더라도 다음은 기본 추가:
- 키보드 포커스 링 (접근성)
- 호버 피드백
- 클릭 가능한 요소는 `cursor: pointer`
- 폼 입력은 라벨 연결 (`<label htmlFor>`)

## 3. 데스크톱: 타겟 폴더 & 파일 ingest

### 3.1 타겟 폴더 지정
- 사용자가 OS 파일 다이얼로그로 로컬 폴더 선택.
- 조직별로 여러 타겟 폴더 등록 가능(예: `회사별/`, `용역/` 분리 폴더).
- 폴더 경로는 OS keychain/암호화된 settings에 저장.
- 권한 확인: 폴더 읽기 권한 없으면 즉시 사용자에게 안내.

### 3.2 변경 감지
- 파일 시스템 watcher(`chokidar` 등)로 추가/수정/삭제 감지.
- 디바운스(2~5초) — 사용자가 저장을 반복하는 동안 매번 ingest하지 않도록.
- 데스크톱 status bar(`StatusBar` 컴포넌트)는 진행 중인 ingest를 실시간 표시 (`ingest 진행: 한미산업 / 차입금명세서.xlsx (3/3)`).

### 3.3 지원 파일 형식
모두 ingest 파이프라인 통과:

| 형식 | 처리 | 비고 |
|------|------|------|
| `.docx` / `.doc` | mammoth/docx 파서, 본문·표·스타일 추출 | |
| `.xlsx` / `.xls` | sheetjs, 시트별 표 구조 보존 | 큰 파일은 시트 단위로 분할 ingest |
| `.pdf` | pdf.js + OCR fallback (스캔본) | 텍스트 레이어 우선, 없으면 OCR |
| `.txt` / `.md` | UTF-8 직접 읽기, 마크다운은 AST 보존 | BOM·인코딩 자동 감지 |
| `.hwp` / `.hwpx` | 별도 파이프라인 (§4 참조) | |
| `.zip` | 안에 있는 지원 형식 자동 추출 | |
| 이미지 | OCR + 메타 보존, 위키 임베드 | |

지원하지 않는 형식은 "원본 보관·메타만 인덱스" 모드로 들어감 (검색은 파일명/태그 기반).

### 3.4 ingest 파이프라인 (백엔드)
디자인 `Screen_Folder` 와 `Screen_Admin` 컴파일 로그가 보여주는 흐름:
```
1. 파일 변경 감지 → 큐 등록
2. 형식별 파서로 텍스트/표/메타 추출
3. (HWP인 경우) PDF 변환 + 표 분리 저장
4. LLM이 회사·연도·업무 등 구조화 태그 추출 (조직별 태그 스키마 적용)
5. 위키 의존성 그래프 갱신: 어떤 위키 노드가 이 파일을 인용/요약할지 결정
6. 위키 노드 자동 컴파일 (예: `/companies/daehan/loans-2025.md`에 차입 60억 항목 추가)
7. 변경 이력에 "자동 컴파일" 기록 + 출처(원본 파일) 백링크 유지
8. 담당자에게 알림 (조직 정책에 따라)
```

### 3.5 인덱싱 상태 노출
- 파일 단위 상태: `대기 / 분석중 / 완료 / 실패` (디자인 `Screen_Upload` 참조)
- LLM 자동 추출 신뢰도(%)를 함께 보여주고, 사용자가 검토·승인 후 위키 컴파일이 실행되도록 분기 가능 (`승인 · 위키 컴파일` 버튼).

## 4. 위키 데이터 모델 & 컴파일

### 4.1 노드 종류
- **수동 작성 노드**: 사용자가 직접 편집 (디자인 `Screen_Wiki`의 본문 영역)
- **자동 컴파일 노드/영역**: ingest 결과로 갱신 (디자인 위키 페이지의 "자동 컴파일 7회" 표기)
- **혼합 노드**: 자동 영역 + 수동 메모 영역이 한 페이지에 공존

### 4.2 자동 영역 보호
- 사용자가 자동 영역을 직접 편집하려 하면 **차단** + "수동 메모" 영역으로 안내 (회계 자료 같은 신뢰성 중요 도메인에서 자동/수동 경계가 흐려지는 것 방지).
- 충돌 발생 시: 사용자에게 선택지 제공(새 버전으로 교체 / 별도 보관 / 무시) — 자동 덮어쓰기 금지.

### 4.3 출처(인용) 무결성
- 모든 자동 컴파일 항목은 원본 파일·라인까지 추적 가능한 인용을 가짐.
- 디자인 `Cite` 컴포넌트가 보여주듯 본문 안에 inline citation으로 표시.
- 출처 누락 페이지는 위키 관리 대시보드(`Screen_Admin`)에서 별도 노출하고 차단.

### 4.4 백링크·의존성 그래프
- 노드 간 백링크는 자동 유지 (디자인 우측 사이드바 "백링크" 카드).
- 외부 링크(법령 URL 등) 깨짐은 정기 점검(디자인 `Screen_Admin`의 "URL 깨짐" 항목).

## 5. 상태 관리

### 5.1 권한 상태
모든 화면 진입 시 다음을 검사:
```
1. 사용자 인증 여부
2. 활성 조직 멤버십
3. 화면별 권한 (조직 내 ACL — 예: 특정 회사 폴더 접근권)
4. 액션별 권한 (편집/다운로드/공유)
```

권한 없음 처리:
- 화면 진입 자체 불가: 404 또는 권한 요청 화면
- 화면 안의 일부 항목 권한 없음: **렌더링 자체 안 함** (회색 처리 X)
- 액션 권한 없음: 버튼 비활성화 + 툴팁

### 5.2 컨텍스트 상태 (데스크톱)
디자인 `RightContext`가 보여주는 자동 컨텍스트(현재 편집 중인 문서의 회사·연도·업무):
- 모든 우측 탭(회사 정보, 관련 위키, 알림, Claude)이 같은 컨텍스트 기반 동작
- 컨텍스트 변경은 디바운스 (500ms 이상 안정될 때만 적용)
- 사용자가 컨텍스트를 고정(pin)할 수 있는 별도 플래그

### 5.3 비동기 처리
- 검색/Q&A는 디바운스 + 취소 가능
- 파일 인덱싱은 서버 큐 → 클라이언트는 폴링 또는 WebSocket
- 동기화 진행은 진행률 표시 (status bar)

## 6. 보안 처리

### 6.1 본문 업로드 정책 (조직 단위)
조직 설정에 따라 셋 중 하나:
- **전체 본문**: 클라우드에 원문 저장
- **임베딩만**: 벡터만 저장, 검색 가능, 본문은 조직 로컬
- **메타만**: 파일명/태그/요약만 저장

데스크톱 앱은 정책을 받아 **클라이언트에서 강제 준수**. 서버를 거치지 않고 로컬에서 처리하는 경로 분기 필요.

### 6.2 민감 분류 (조직·하위 단위)
- 조직 내 특정 회사/프로젝트를 "민감"으로 분류 시:
  - 추가 인증 게이트
  - 화면 캡처 시도 차단/로깅 (OS 지원 시)
  - 클립보드 복사 제한 옵션
  - 캐시 비활성화 옵션

### 6.3 출처 표시 (LLM 답변)
LLM 답변은 **반드시** 출처 블록과 함께 렌더링 (디자인 `Screen_Search`의 "위키 통합 답변", `Screen_Wiki`의 출처 섹션 참조):
- 출처 없는 답변은 별도 색상/배경으로 구분
- 일반 지식 답변은 "내부 자료에 없음" 명시
- 사용자가 답변 복사 시 출처 함께 복사

### 6.4 활동 로그
다음 행위는 **모두** 로그 (조직 단위로 보관):
- 페이지 진입 (view)
- 편집 (edit, diff 포함)
- 다운로드 (download)
- 파일 업로드/ingest (upload, ingest)
- 공유/권한 변경 (share, permission_change)
- 로그인/로그아웃 (login)
- 화면 캡처 시도 (screenshot_attempt)
- LLM 질의 (ai_query) — 질문/응답/사용 컨텍스트

## 7. 한글(.hwp) 처리

타겟 폴더 안에 한글 파일이 흔하게 들어 있는 한국 vertical 대응 — 디자인 `Screen_Folder`의 "HWP 처리 파이프라인" 참조.

### 7.1 표시
- `.hwp`/`.hwpx` 파싱 (한컴 SDK 또는 오픈소스 파서, 서버 측 처리 권장)
- 미리보기는 텍스트 + 표 구조 유지
- 완벽한 레이아웃 재현은 포기, 대신 "원본 파일 열기" 버튼 제공

### 7.2 표 추출
- 한글 문서의 표를 별도 추출
- "엑셀로 내보내기" 버튼: 사용자가 클릭하면 xlsx 변환 파일 다운로드
- 인터뷰 페인포인트("한글 표를 엑셀에 붙이기 어렵다") 직접 대응

### 7.3 검색 가능성
- 본문 텍스트는 다른 형식과 동일하게 검색 인덱스에 들어가야 함
- 한글 형태소 분석기 사용 (Mecab, Nori 등)
- 스캔본 HWP는 OCR fallback 필수

## 8. 데이터 동기화

### 8.1 충돌 해결
같은 파일의 다른 버전 발견 시:
- 사용자에게 선택지 제공: 새 버전으로 교체 / 별도 보관 / 무시
- 자동 처리는 **하지 않음** (회계 자료 같은 신뢰성 중요 데이터에서 자동 덮어쓰기는 위험)

### 8.2 자동 추출 영역
- Wiki의 "재무 추세" 같은 자동 영역은 ingest 결과로 갱신
- 갱신 시 Wiki 변경 이력에 "자동 갱신" 표시
- 사용자가 수동으로 추출 영역 편집 시도 → 차단 + "수동 메모" 영역으로 안내

### 8.3 외부 시스템 연동 (vertical 옵션)
*도메인별 부록 — 회계 vertical에서 사용. 코어에는 없음.*
- 더존/위하고: 회사가 자체 API 제공하지 않으면 **사용자가 수동 export → 타겟 폴더에 저장 → ingest**
- 홈택스: OAuth 또는 공동인증서 연동
- 동기화 실패는 알림으로 노출 (조용히 실패하면 안 됨)

## 9. 검색 동작

### 9.1 통합 검색바
입력에 따라 분기:
- 일반 텍스트: Wiki + 파일 통합
- 따옴표 포함: 정확 일치
- 의문문: Q&A로 라우팅 제안
- `/` 시작: 명령 모드 (데스크톱)

### 9.2 결과 렌더링
- 결과는 "Wiki 노드", "파일", "조직 내 분류(예: 회사)" 같은 그룹으로 묶음
- 각 결과는 매칭 위치 하이라이트
- 검색어가 의미 검색에서만 매칭된 경우 "유사 결과" 라벨

### 9.3 권한 필터링
- **서버에서** 권한 필터링 후 반환
- 클라이언트가 필터링하면 권한 없는 항목 존재 자체가 노출됨
- 권한으로 차단된 결과는 그냥 결과 없음으로 표시(개수에서 빼지 말 것)

## 10. LLM 통합

### 10.1 RAG 구조
1. 사용자 질의 → 임베딩
2. Vector DB에서 관련 Wiki/파일 검색 (조직 + 권한 필터링)
3. 검색 결과를 컨텍스트로 LLM 호출
4. 응답 + 출처를 함께 반환

조직 위키의 데이터만 컨텍스트로 사용 — 다른 조직 데이터는 절대 새지 않도록 검색 단계에서 차단.

### 10.2 컨텍스트 자동 주입 (데스크톱)
디자인 `RightAITab`이 보여주듯, 현재 편집 중인 문서의 회사·연도·업무 태그가 자동 컨텍스트 칩으로 표시되고 LLM 호출 시 함께 전달됨.
- 사용자가 컨텍스트 칩을 제거/추가 가능
- "+ 영구조서 6건"처럼 자동 확장된 참조 자료 개수도 노출

### 10.3 모델 선택
- 조직 설정에서 모델 선택 (보안 등급별)
- 민감 분류된 항목은 더 강한 격리 모델 사용 옵션
- 모델별 API 키는 조직 단위로 관리

### 10.4 응답 검증
- 응답에 포함된 사실 주장은 출처 매핑
- 출처에서 근거를 찾을 수 없는 주장은 "내부 자료 외" 표시
- 도메인 vertical(예: 세법)이 활성화된 경우 추가 경고 (예: "최신 개정 확인 필수")

## 11. 컴포넌트 구현 권장 라이브러리

### 11.1 웹 대시보드
- **프레임워크**: Next.js 또는 Remix
- **블록 에디터**: Tiptap, Lexical
- **데이터 테이블**: TanStack Table + 가상 스크롤
- **차트**: Recharts, Visx (디자인의 sparkline은 inline SVG로 직접 그리는 패턴 — `Spark` 컴포넌트 참고)
- **상태**: TanStack Query (서버 상태) + Zustand (UI 상태)

### 11.2 데스크톱
- **셸**: Electron (성숙) 또는 Tauri (가벼움)
- **OS 통합**: 트레이, 전역 단축키(`Ctrl+J`로 LLM 호출 등), 포그라운드 창 인식, 파일 시스템 watcher 모듈 분리
- **UI**: 웹과 컴포넌트 공유 (모노레포 권장)
- **로컬 캐시**: SQLite + 암호화 (조직 토큰 격리)

### 11.3 공통
- **타입**: TypeScript 필수
- **스타일**: Tailwind 또는 CSS Modules — 디자인 파일은 inline-CSS-in-JS 스타일이지만 production은 모듈로 분리
- **테스트**: 권한 분기/보안 동작은 반드시 테스트

## 12. 디자인 파일을 컴포넌트로 분해할 때

### 12.1 분해 단위
- **페이지**: 라우트 단위 (예: `/wiki/companies/:id`, `/orgs/:orgId/dashboard`)
- **레이아웃**: 사이드바, 헤더 등 공유 영역 (`Shell`, `LeftExplorer`, `RightContext`)
- **블록**: Wiki 본문 블록 단위
- **위젯**: 카드, 차트, 테이블 행 등

### 12.2 props 설계
- 권한·활성 조직은 prop으로 받지 말고 컨텍스트(Context)에서
- 데이터는 명시적 prop, 표시 옵션은 default 값
- 액션 핸들러는 명시적 prop (테스트 용이)

### 12.3 디자인에 없는 컴포넌트
디자인 파일에 빠져 있지만 반드시 추가해야 하는 컴포넌트:
- `<EmptyState>` (검색 결과 없음, 데이터 없음, 타겟 폴더 미설정)
- `<LoadingSkeleton>` (각 화면별)
- `<ErrorBoundary>`
- `<AuthGate>` (로그인 검사)
- `<OrgGate>` (조직 멤버십 검사)
- `<PermissionGate>` (액션 권한 검사 래퍼)
- `<ConfirmDialog>` (삭제, 권한 변경 등 위험 액션)
- `<ToastNotification>` (알림 표시)
- `<TargetFolderPicker>` (데스크톱 전용)
- `<IngestProgress>` (status bar에 들어갈 진행률 컴포넌트)

## 13. 체크리스트 (구현 완료 전 확인)

화면 하나를 구현 완료라고 부르기 전 확인:

- [ ] 모든 데이터가 모델에서 오고 목업이 남아있지 않음
- [ ] 빈 상태/로딩/에러 처리됨
- [ ] 인증·조직 멤버십·권한 검사가 진입과 액션 양쪽에 있음
- [ ] 활성 조직 컨텍스트로만 데이터를 호출 (다른 조직 데이터 누출 없음)
- [ ] 모든 액션이 활동 로그에 남음
- [ ] 키보드 접근성 동작
- [ ] LLM 응답이 있다면 출처 블록 함께 렌더링
- [ ] 다중 형식 파일(docx/xlsx/pdf/hwp/txt/md)이 들어와도 깨지지 않음
- [ ] 권한 없는 항목이 흔적 없이 사라짐
- [ ] 양 클라이언트(웹·데스크톱)가 같은 데이터를 보여주면 같은 렌더링
- [ ] 민감 분류 항목 진입 시 추가 인증 동작
- [ ] 데스크톱: 타겟 폴더 미설정 상태에서도 명확한 안내 표시
